# Generated by `MOSAiC_AYiL.write_parameter_file`; edit `src/params.jl`.

[potential_temperature_reference_pressure]
value = 100000.0
type = "float"
description = "DALES `pref0`, the reference pressure of its Exner function; equals the ClimaParams default, set so it cannot drift"

[von_karman_constant]
value = 0.4
type = "float"
description = "DALES `fkar`, used by its Monin-Obukhov surface layer; equals the ClimaParams default, set so it cannot drift"

[gas_constant_dry_air]
value = 287.04
type = "float"
description = "DALES `rd`; ClimaParams default 287.0 (+0.014%)"

[gas_constant_vapor]
value = 461.5
type = "float"
description = "DALES `rv`; equals the ClimaParams default, set so it cannot drift"

[molar_mass_dry_air]
value = 0.028966206103678928
type = "float"
description = "gas_constant / 287.04, so the molar mass matches `gas_constant_dry_air`"

[molar_mass_water]
value = 0.018016164247020586
type = "float"
description = "gas_constant / 461.5, so the molar mass matches `gas_constant_vapor`"

[adiabatic_exponent_dry_air]
value = 0.2858964143426295
type = "float"
description = "287.04 / 1004, matching `gas_constant_dry_air` / `isobaric_specific_heat_dry_air`"

[latent_heat_vaporization_at_reference]
value = 2.53e6
type = "float"
description = "DALES `rlv`; ClimaParams default 2.5008e6 (+1.168%)"

[temperature_water_freeze]
value = 273.16
type = "float"
description = "DALES `tmelt`; ClimaParams default 273.15"

[density_liquid_water]
value = 998.0
type = "float"
description = "DALES `rhow`; ClimaParams default 1000 (-0.2%)"

[latent_heat_sublimation_at_reference]
value = 2.834e6
type = "float"
description = "DALES SB3 `rlvi`, the value its microphysics used; `riv = 2.84e6` in modglobal is declared but never referenced. ClimaParams default 2.8344e6 (-0.014%)"

[gravitational_acceleration]
value = 9.81
type = "float"
description = "DALES `grav`; equals the ClimaParams default, set so it cannot drift"

[stefan_boltzmann_constant]
value = 5.67e-8
type = "float"
description = "DALES `boltz`; equals the ClimaParams default, set so it cannot drift"

[isobaric_specific_heat_dry_air]
value = 1004.0
type = "float"
description = "DALES `cp`; ClimaParams default 1004.5 (-0.050%)"
